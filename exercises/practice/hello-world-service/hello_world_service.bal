import ballerina/http;

// Implement a HTTP service listening on port 9090 with the root path "/"
service {

    // Add a GET resource called "greeting" that returns the "Hello, World!" message
    resource {

    }
}
